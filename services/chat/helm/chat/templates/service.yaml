apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat.fullname" . }}
  labels:
    app: {{ include "chat.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "chat.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
